# Strategy Machine Rule Builder

The rule-builder module converts hypotheses into machine-readable rule sets. Subjective language is rejected. New improvements create new versions and preserve old versions by event reference.

Rule sets include entry, exit, stop-loss, take-profit, position sizing, session, volatility, spread, regime, news blackout, instrument, and timeframe constraints.

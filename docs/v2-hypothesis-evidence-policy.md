# V2 Hypothesis Evidence Policy

Generation fails closed when evidence is insufficient, lineage is incomplete, a condition depends on future data, a circular definition is detected, or complexity is excessive. Duplicate hypotheses are detected by canonical condition ordering and objective outcome/baseline fingerprints.

# V2 Rule Compiler

The compiler accepts ready hypotheses and emits immutable strategy definitions only when stop loss, take profit, invalidation, position sizing, cost model, supported operators, and required features are valid.

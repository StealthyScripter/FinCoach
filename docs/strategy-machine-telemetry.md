# Strategy Machine Operational Telemetry

Telemetry monitors data freshness, pattern detector throughput, hypothesis and experiment throughput, backtest health, validation pass/fail, forward-test health, journal completion, demo execution safety blocks, and provider reliability.

External reliability fields remain nullable when credentials are not configured.

# V2 Security Boundaries

Secrets are redacted before operational output. Live broker endpoints are rejected. Practice endpoint allowlists are explicit. V2 reliability code does not read credentials from environment variables.

# V2 Fundamentals

The fundamentals module structures economic, corporate, snapshot, and extracted textual evidence. It stores source timestamps and publication timestamps so snapshots are point-in-time and cannot include evidence published after the effective timestamp.

AI extraction is bounded to structured claims with citation, model version, confidence, uncertainty, expiration, and verification status. Unverified or uncited claims are rejected and never become executable output.

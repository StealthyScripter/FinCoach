import type { StrategyDefinition } from "../rules";
export type ForwardTestStatus = "blocked" | "monitoring" | "completed" | "failed" | "cancelled";
export type DemoVerification = { demoOnly: true; environment: "practice" | "sandbox" | "paper"; accountMode: "practice" | "sandbox" | "paper"; verifiedAt: string };
export type ForwardMarketSnapshot = { snapshotId: string; symbol: string; timestamp: string; bid: number; ask: number; spread: number; fresh: boolean; contextEventId: string; lineageEventIds: string[] };
export type ForwardTestRecord = { forwardTestId: string; schemaVersion: "fincoach.v2.forward-test.1"; strategyId: string; strategyVersion: number; courtCaseId: string; rankingId: string; status: ForwardTestStatus; demoVerification: DemoVerification; snapshot: ForwardMarketSnapshot; ruleEvaluation: Record<string, unknown>; reason: string; counterargument: string; expectedR: number; risk: number; createdAt: string; lineageEventIds: string[]; correlationId: string; causationId: string | null };
export type ForwardTestRequest = { strategy: StrategyDefinition; courtCaseId: string; courtVerdict: "approve_for_forward_test" | "approve_for_replay" | "watch" | "reject" | "revise"; rankingId: string; snapshot: ForwardMarketSnapshot; demoVerification: DemoVerification; killSwitchActive: boolean; reason: string; counterargument: string; expectedR: number; risk: number; lineageEventIds: string[]; correlationId: string; causationId: string | null };

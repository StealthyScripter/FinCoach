import type { NormalizedCandle } from "../market-data";
import type { StrategyDefinition } from "../rules";
export type BacktestTrade = { tradeId: string; entryAt: string; exitAt: string; side: "buy" | "sell"; entry: number; exit: number; r: number; cost: number; mfe: number; mae: number };
export type BacktestMetrics = { netProfit: number; grossProfit: number; grossLoss: number; profitFactor: number | null; expectancy: number; averageR: number; medianR: number; winRate: number; lossRate: number; maxDrawdown: number; tradeCount: number; sampleDepth: number; costSensitivity: number; stability: number };
export type BacktestResult = { backtestId: string; experimentId: string; strategyId: string; strategyVersion: number; datasetFingerprint: string; engineVersion: string; costModelVersion: string; fillModelVersion: string; randomSeed: string; partitions: Array<{ name: string; metrics: BacktestMetrics }>; aggregateMetrics: BacktestMetrics; trades: BacktestTrade[]; warnings: string[]; lineageEventIds: string[]; status: "completed" | "failed" | "invalid"; createdAt: string; correlationId: string; causationId: string | null };
export type BacktestInput = { experimentId: string; strategy: StrategyDefinition; candles: NormalizedCandle[]; randomSeed: string; lineageEventIds: string[]; correlationId: string; causationId: string | null; spread: number; commissionPerTrade: number; slippage: number };
